---
phase: 06-transition-dataset-and-baseline-benchmarks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mimic_sepsis_rl/datasets/transitions.py
  - src/mimic_sepsis_rl/datasets/replay_buffer.py
  - src/mimic_sepsis_rl/cli/build_transitions.py
  - tests/datasets/test_transitions.py
autonomous: true
requirements:
  - DATA-01
user_setup: []
must_haves:
  truths:
    - "Researcher can export `(s_t, a_t, r_t, s_t+1, done)` transitions and episode replay structures."
    - "Transition datasets preserve the frozen split, action, and reward contracts."
  artifacts:
    - path: "src/mimic_sepsis_rl/datasets/transitions.py"
      provides: "Transition dataset generation"
      min_lines: 60
    - path: "src/mimic_sepsis_rl/datasets/replay_buffer.py"
      provides: "Episode-aware replay buffer serialization"
      min_lines: 40
    - path: "tests/datasets/test_transitions.py"
      provides: "Regression coverage for transition shape and done handling"
      min_lines: 25
  key_links:
    - from: "src/mimic_sepsis_rl/datasets/transitions.py"
      to: "src/mimic_sepsis_rl/datasets/replay_buffer.py"
      via: "Transition rows are convertible into replay-ready episode buffers"
---

<objective>
Create the shared transition dataset contract for all baselines and RL trainers.

Purpose: Convert the frozen state, action, and reward surfaces into replay-ready artifacts that no downstream model can redefine.
Output: Transition builder, replay buffer serializer, CLI entrypoint, and regression tests.
</objective>

<execution_context>
@/home/furkan/.codex/get-shit-done/workflows/execute-plan.md
@/home/furkan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/phases/06-transition-dataset-and-baseline-benchmarks/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement transition and replay-buffer builders</name>
  <files>src/mimic_sepsis_rl/datasets/transitions.py, src/mimic_sepsis_rl/datasets/replay_buffer.py</files>
  <action>Implement transition-row generation from state, action, reward, and episode step artifacts. Preserve episode boundaries, `done` semantics, and split-aware filtering. Add replay-buffer serialization helpers so later trainers can load a shared dataset contract without recomputing transitions.</action>
  <verify>python -m mimic_sepsis_rl.cli.build_transitions --dry-run</verify>
  <done>Transition artifacts and replay-ready buffers can be produced from the frozen MDP contract.</done>
</task>

<task type="auto">
  <name>Task 2: Add CLI and contract tests for dataset integrity</name>
  <files>src/mimic_sepsis_rl/cli/build_transitions.py, tests/datasets/test_transitions.py</files>
  <action>Create a CLI entrypoint for transition export and tests that verify step-to-step alignment, `done` handling, and required metadata linking back to split/action/reward versions. Guard against accidental recomputation that ignores manifest boundaries.</action>
  <verify>pytest -q tests/datasets/test_transitions.py && python -m mimic_sepsis_rl.cli.build_transitions --dry-run</verify>
  <done>The shared transition contract is invokable, test-covered, and safe for multiple downstream trainers.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest -q tests/datasets/test_transitions.py` passes
- [ ] `python -m mimic_sepsis_rl.cli.build_transitions --dry-run` completes with split-aware metadata
- [ ] Transition outputs preserve episode boundaries and `done` semantics
</verification>

<success_criteria>

- All tasks completed
- DATA-01 is satisfied through replay-ready transition and buffer artifacts
- Later baseline and RL plans can load one shared dataset contract

</success_criteria>

<output>
After completion, create `.planning/phases/06-transition-dataset-and-baseline-benchmarks/06-01-SUMMARY.md`
</output>
