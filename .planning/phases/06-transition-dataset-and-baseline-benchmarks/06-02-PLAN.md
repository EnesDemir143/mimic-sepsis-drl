---
phase: 06-transition-dataset-and-baseline-benchmarks
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/mimic_sepsis_rl/baselines/clinician.py
  - src/mimic_sepsis_rl/baselines/no_treatment.py
  - src/mimic_sepsis_rl/baselines/behavior_cloning.py
  - tests/baselines/test_baselines.py
  - docs/baseline_benchmarks.md
autonomous: true
requirements:
  - BASE-01
user_setup: []
must_haves:
  truths:
    - "Researcher can benchmark clinician behavior, no-treatment, and behavior cloning baselines on the same dataset definition."
    - "Baseline outputs are directly comparable to later RL runs."
  artifacts:
    - path: "src/mimic_sepsis_rl/baselines/behavior_cloning.py"
      provides: "Behavior cloning reference baseline"
      min_lines: 50
    - path: "tests/baselines/test_baselines.py"
      provides: "Coverage for baseline dataset compatibility"
      min_lines: 25
    - path: "docs/baseline_benchmarks.md"
      provides: "Baseline benchmark interpretation and output schema"
      min_lines: 25
  key_links:
    - from: "src/mimic_sepsis_rl/baselines/clinician.py"
      to: "src/mimic_sepsis_rl/datasets/transitions.py"
      via: "Clinician baseline consumes the shared transition contract"
    - from: "src/mimic_sepsis_rl/baselines/behavior_cloning.py"
      to: "src/mimic_sepsis_rl/datasets/replay_buffer.py"
      via: "Behavior cloning trains on the same replay-ready data surface"
---

<objective>
Establish the comparison baselines that all RL algorithms must beat or contextualize.

Purpose: Finish Phase 6 with cheap, reproducible baseline runs on the exact same dataset contract used later by CQL, BCQ, and IQL.
Output: Clinician, no-treatment, and behavior-cloning baselines with tests and benchmark documentation.
</objective>

<execution_context>
@/home/furkan/.codex/get-shit-done/workflows/execute-plan.md
@/home/furkan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/phases/06-transition-dataset-and-baseline-benchmarks/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement baseline runners on the shared transition contract</name>
  <files>src/mimic_sepsis_rl/baselines/clinician.py, src/mimic_sepsis_rl/baselines/no_treatment.py, src/mimic_sepsis_rl/baselines/behavior_cloning.py</files>
  <action>Implement baseline runners that consume the frozen transition dataset without redefining preprocessing. Include a clinician-action replay baseline, a no-treatment baseline, and a behavior-cloning training/evaluation surface that will later be compared directly against RL policies.</action>
  <verify>python -m mimic_sepsis_rl.baselines.behavior_cloning --dry-run</verify>
  <done>All required baselines can be invoked against the shared dataset contract.</done>
</task>

<task type="auto">
  <name>Task 2: Add compatibility tests and benchmark reporting docs</name>
  <files>tests/baselines/test_baselines.py, docs/baseline_benchmarks.md</files>
  <action>Write tests that confirm baseline runners accept the shared dataset contract and emit comparable outputs. Document benchmark schemas, expected metrics, and the interpretation boundary between simple baselines and later RL policies.</action>
  <verify>pytest -q tests/baselines/test_baselines.py</verify>
  <done>Baseline runs are test-covered, documented, and ready to contextualize RL training.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest -q tests/baselines/test_baselines.py` passes
- [ ] `python -m mimic_sepsis_rl.baselines.behavior_cloning --dry-run` completes using the shared transition dataset
- [ ] Baseline docs describe outputs in a format comparable to later RL runs
</verification>

<success_criteria>

- All tasks completed
- BASE-01 is satisfied with reusable baseline implementations and docs
- Phase 7 can begin from a stable dataset contract with known benchmark outputs

</success_criteria>

<output>
After completion, create `.planning/phases/06-transition-dataset-and-baseline-benchmarks/06-02-SUMMARY.md`
</output>
