---
phase: 08-comparative-offline-rl-experiments
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/mimic_sepsis_rl/training/bcq.py
  - src/mimic_sepsis_rl/training/iql.py
  - src/mimic_sepsis_rl/training/comparison.py
  - tests/training/test_comparison_runs.py
  - docs/model_comparison.md
autonomous: true
requirements:
  - RL-02
  - RL-03
user_setup: []
must_haves:
  truths:
    - "Researcher can train BCQ and IQL on the same contract used by CQL."
    - "Every model run records checkpoints, curves, and config provenance for fair comparison."
  artifacts:
    - path: "src/mimic_sepsis_rl/training/bcq.py"
      provides: "BCQ training pipeline"
      min_lines: 50
    - path: "src/mimic_sepsis_rl/training/iql.py"
      provides: "IQL training pipeline"
      min_lines: 50
    - path: "src/mimic_sepsis_rl/training/comparison.py"
      provides: "Comparison artifact aggregation and reporting"
      min_lines: 40
    - path: "tests/training/test_comparison_runs.py"
      provides: "Coverage for shared artifact schemas across algorithms"
      min_lines: 25
  key_links:
    - from: "src/mimic_sepsis_rl/training/bcq.py"
      to: "src/mimic_sepsis_rl/training/experiment_runner.py"
      via: "BCQ training is launched through the shared runner"
    - from: "src/mimic_sepsis_rl/training/iql.py"
      to: "src/mimic_sepsis_rl/training/experiment_runner.py"
      via: "IQL training is launched through the shared runner"
---

<objective>
Implement BCQ and IQL on the shared training surface and standardize comparison artifacts.

Purpose: Finish Phase 8 with fair algorithm-to-algorithm comparisons and complete run provenance.
Output: BCQ and IQL trainers, comparison aggregation utilities, tests, and model-comparison docs.
</objective>

<execution_context>
@/home/furkan/.codex/get-shit-done/workflows/execute-plan.md
@/home/furkan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/phases/08-comparative-offline-rl-experiments/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement BCQ and IQL trainers on the shared experiment surface</name>
  <files>src/mimic_sepsis_rl/training/bcq.py, src/mimic_sepsis_rl/training/iql.py</files>
  <action>Implement BCQ and IQL training modules that plug into the shared experiment runner, reuse the CQL-era dataset and runtime abstractions, and emit checkpoints and metrics under a common schema. Avoid any code path that bypasses the shared dataset contract or introduces backend-specific divergence outside the runtime layer.</action>
  <verify>python -m mimic_sepsis_rl.training.experiment_runner --algorithm bcq --dry-run && python -m mimic_sepsis_rl.training.experiment_runner --algorithm iql --dry-run</verify>
  <done>BCQ and IQL can be launched on the same contract and runtime surface as CQL.</done>
</task>

<task type="auto">
  <name>Task 2: Standardize comparison artifacts, tests, and docs</name>
  <files>src/mimic_sepsis_rl/training/comparison.py, tests/training/test_comparison_runs.py, docs/model_comparison.md</files>
  <action>Implement utilities that aggregate per-run checkpoints, curves, configs, and key metrics into a shared comparison schema. Add tests that verify consistent artifact shapes across CQL, BCQ, and IQL, and document how to interpret comparison outputs without confusing algorithm differences with data-contract drift.</action>
  <verify>pytest -q tests/training/test_comparison_runs.py</verify>
  <done>Comparative experiment outputs are standardized, test-covered, and ready for final evaluation workflows.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest -q tests/training/test_comparison_runs.py` passes
- [ ] BCQ and IQL dry runs complete through the shared experiment runner
- [ ] Comparison docs describe checkpoints, curves, and config provenance for all algorithms
</verification>

<success_criteria>

- All tasks completed
- RL-02 and RL-03 are satisfied with fair multi-algorithm training and shared run artifacts
- Phase 9 can consume model outputs without per-algorithm special cases

</success_criteria>

<output>
After completion, create `.planning/phases/08-comparative-offline-rl-experiments/08-02-SUMMARY.md`
</output>
