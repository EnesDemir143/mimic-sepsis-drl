---
phase: 08-comparative-offline-rl-experiments
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mimic_sepsis_rl/training/registry.py
  - src/mimic_sepsis_rl/training/experiment_runner.py
  - configs/training/bcq.yaml
  - configs/training/iql.yaml
  - tests/training/test_algorithm_registry.py
autonomous: true
requirements:
  - RL-02
user_setup: []
must_haves:
  truths:
    - "BCQ and IQL can be configured against the same dataset, reward, and action contract used by CQL."
    - "Algorithm launches share one training interface and runtime abstraction."
  artifacts:
    - path: "src/mimic_sepsis_rl/training/registry.py"
      provides: "Algorithm registry for CQL, BCQ, and IQL"
      min_lines: 40
    - path: "src/mimic_sepsis_rl/training/experiment_runner.py"
      provides: "Shared run launcher for offline RL experiments"
      min_lines: 50
    - path: "tests/training/test_algorithm_registry.py"
      provides: "Coverage for algorithm selection and shared runtime wiring"
      min_lines: 25
  key_links:
    - from: "src/mimic_sepsis_rl/training/experiment_runner.py"
      to: "src/mimic_sepsis_rl/training/registry.py"
      via: "Experiment runs resolve algorithm-specific trainers through one registry"
---

<objective>
Create the shared experiment surface for multi-algorithm offline RL comparisons.

Purpose: Keep BCQ and IQL additions fair by forcing them through the same dataset and runtime entrypoints used by CQL.
Output: Algorithm registry, experiment runner, BCQ/IQL configs, and regression tests for shared wiring.
</objective>

<execution_context>
@/home/furkan/.codex/get-shit-done/workflows/execute-plan.md
@/home/furkan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/phases/08-comparative-offline-rl-experiments/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement shared algorithm registry and run launcher</name>
  <files>src/mimic_sepsis_rl/training/registry.py, src/mimic_sepsis_rl/training/experiment_runner.py</files>
  <action>Create a shared registry and experiment runner that can launch CQL, BCQ, and IQL against the same replay dataset, reward version, and runtime abstraction. Keep configuration-driven algorithm selection and prevent per-algorithm code paths from redefining dataset loading or device handling.</action>
  <verify>python -m mimic_sepsis_rl.training.experiment_runner --list-algorithms</verify>
  <done>The project has one reusable interface for launching offline RL experiments.</done>
</task>

<task type="auto">
  <name>Task 2: Add BCQ and IQL configs plus registry regression tests</name>
  <files>configs/training/bcq.yaml, configs/training/iql.yaml, tests/training/test_algorithm_registry.py</files>
  <action>Create baseline configs for BCQ and IQL that mirror the CQL data contract and runtime keys. Add tests verifying that algorithm registration, config resolution, and runtime selection remain consistent across all supported trainers.</action>
  <verify>pytest -q tests/training/test_algorithm_registry.py</verify>
  <done>BCQ and IQL can be launched through the same shared experiment surface used by CQL.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest -q tests/training/test_algorithm_registry.py` passes
- [ ] `python -m mimic_sepsis_rl.training.experiment_runner --list-algorithms` includes CQL, BCQ, and IQL
- [ ] BCQ and IQL configs reference the shared dataset and runtime abstraction keys
</verification>

<success_criteria>

- All tasks completed
- RL-02 is partially satisfied through a fair, shared experiment-launch surface
- Comparative training can proceed without duplicating the CQL stack

</success_criteria>

<output>
After completion, create `.planning/phases/08-comparative-offline-rl-experiments/08-01-SUMMARY.md`
</output>
