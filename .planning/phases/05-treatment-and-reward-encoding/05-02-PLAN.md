---
phase: 05-treatment-and-reward-encoding
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/mimic_sepsis_rl/mdp/rewards.py
  - src/mimic_sepsis_rl/mdp/reward_models.py
  - tests/mdp/test_rewards.py
  - docs/reward_spec.md
autonomous: true
requirements:
  - RWD-01
user_setup: []
must_haves:
  truths:
    - "Researcher can compute terminal 90-day mortality rewards and intermediate shaping rewards for every transition."
    - "Reward distributions are inspectable before training begins."
  artifacts:
    - path: "src/mimic_sepsis_rl/mdp/rewards.py"
      provides: "Reward calculation and shaping policy"
      min_lines: 60
    - path: "docs/reward_spec.md"
      provides: "Written reward definition for methods reporting"
      min_lines: 30
    - path: "tests/mdp/test_rewards.py"
      provides: "Coverage for terminal and shaping reward behavior"
      min_lines: 25
  key_links:
    - from: "src/mimic_sepsis_rl/mdp/rewards.py"
      to: "docs/reward_spec.md"
      via: "Implementation and written reward policy stay aligned"
---

<objective>
Implement the project's reward contract and diagnostics.

Purpose: Complete Phase 5 by turning outcomes and clinical deltas into a stable reward surface for all later baselines and policies.
Output: Reward module, typed reward outputs, regression tests, and human-readable reward specification.
</objective>

<execution_context>
@/home/furkan/.codex/get-shit-done/workflows/execute-plan.md
@/home/furkan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/phases/05-treatment-and-reward-encoding/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement terminal and shaped reward calculators</name>
  <files>src/mimic_sepsis_rl/mdp/rewards.py, src/mimic_sepsis_rl/mdp/reward_models.py</files>
  <action>Implement terminal 90-day mortality reward logic and conservative intermediate shaping around SOFA deltas, with configuration hooks for lactate and MAP terms. Keep reward versioning explicit so future ablations can compare sparse and shaped variants without touching training code.</action>
  <verify>python -m mimic_sepsis_rl.mdp.rewards --dry-run</verify>
  <done>Reward tables can be generated deterministically for the current dataset contract.</done>
</task>

<task type="auto">
  <name>Task 2: Add reward diagnostics, documentation, and regression tests</name>
  <files>tests/mdp/test_rewards.py, docs/reward_spec.md</files>
  <action>Write tests covering terminal outcome handling, SOFA delta behavior, and shaped-term edge cases. Document the exact reward formula, configurable shaping parameters, and expected diagnostic outputs such as reward histograms and summary statistics.</action>
  <verify>pytest -q tests/mdp/test_rewards.py</verify>
  <done>The reward contract is documented, test-covered, and ready to feed transition export.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest -q tests/mdp/test_rewards.py` passes
- [ ] `python -m mimic_sepsis_rl.mdp.rewards --dry-run` generates reward outputs without missing-field errors
- [ ] `docs/reward_spec.md` records both terminal reward and shaping-policy behavior
</verification>

<success_criteria>

- All tasks completed
- RWD-01 is satisfied through an explicit and testable reward contract
- Phase 6 can consume reward outputs without redefining clinical reward logic

</success_criteria>

<output>
After completion, create `.planning/phases/05-treatment-and-reward-encoding/05-02-SUMMARY.md`
</output>
