---
phase: 04-state-representation-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/mimic_sepsis_rl/mdp/features/builder.py
  - src/mimic_sepsis_rl/mdp/preprocessing.py
  - tests/mdp/test_state_builder.py
  - tests/mdp/test_preprocessing.py
autonomous: true
requirements:
  - STAT-02
  - STAT-03
user_setup: []
must_haves:
  truths:
    - "Researcher can apply forward-fill, fallback median imputation, and optional missingness flags with a documented policy."
    - "Clipping and normalization fit on the train split only and can be replayed on validation and test data."
  artifacts:
    - path: "src/mimic_sepsis_rl/mdp/features/builder.py"
      provides: "Step-level state vector construction"
      min_lines: 60
    - path: "src/mimic_sepsis_rl/mdp/preprocessing.py"
      provides: "Train-only fit preprocessing artifacts and transforms"
      min_lines: 50
    - path: "tests/mdp/test_preprocessing.py"
      provides: "Coverage for train-only fitting and fallback rules"
      min_lines: 25
  key_links:
    - from: "src/mimic_sepsis_rl/mdp/features/builder.py"
      to: "src/mimic_sepsis_rl/mdp/preprocessing.py"
      via: "Built state vectors are transformed through frozen preprocessing artifacts"
---

<objective>
Implement deterministic state building with leakage-safe preprocessing.

Purpose: Finish Phase 4 by making the feature contract executable as model-ready continuous state vectors.
Output: State builder, preprocessing module, and tests for imputation, missingness flags, clipping, and train-only normalization.
</objective>

<execution_context>
@/home/furkan/.codex/get-shit-done/workflows/execute-plan.md
@/home/furkan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/phases/04-state-representation-pipeline/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement state vector builder with documented imputation order</name>
  <files>src/mimic_sepsis_rl/mdp/features/builder.py, tests/mdp/test_state_builder.py</files>
  <action>Build the step-level state vector assembler so it consumes the feature dictionary, episode grid, and split manifests. Implement the project's documented imputation order: forward fill first, median fallback when no previous value exists, and optional missingness flags where configured. Add tests for deterministic output and edge cases at episode boundaries.</action>
  <verify>pytest -q tests/mdp/test_state_builder.py</verify>
  <done>State vectors can be built repeatably from episode steps with explicit missing-data handling.</done>
</task>

<task type="auto">
  <name>Task 2: Implement train-only preprocessing fit and transform utilities</name>
  <files>src/mimic_sepsis_rl/mdp/preprocessing.py, tests/mdp/test_preprocessing.py</files>
  <action>Create clipping, winsorization if used, and normalization utilities that fit only on train manifests and serialize artifacts for reuse. Tests must fail if validation or test rows influence fitted statistics or if transforms cannot be replayed deterministically.</action>
  <verify>pytest -q tests/mdp/test_preprocessing.py</verify>
  <done>Preprocessing artifacts are fit-on-train, reusable, and protected against leakage regressions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest -q tests/mdp/test_state_builder.py tests/mdp/test_preprocessing.py` passes
- [ ] Train-only preprocessing tests prove validation/test rows do not affect fitted artifacts
- [ ] State builder outputs remain deterministic for the same episode windows and split manifests
</verification>

<success_criteria>

- All tasks completed
- STAT-02 and STAT-03 are satisfied with explicit and test-covered preprocessing behavior
- Phase 5 can consume model-ready state vectors without redefining missing-data logic

</success_criteria>

<output>
After completion, create `.planning/phases/04-state-representation-pipeline/04-02-SUMMARY.md`
</output>
