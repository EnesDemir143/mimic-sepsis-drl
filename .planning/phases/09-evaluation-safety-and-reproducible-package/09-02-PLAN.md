---
phase: 09-evaluation-safety-and-reproducible-package
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - src/mimic_sepsis_rl/evaluation/ablations.py
  - src/mimic_sepsis_rl/reporting/package.py
  - tests/evaluation/test_ablation_registry.py
  - tests/reporting/test_repro_bundle.py
  - docs/reproducibility.md
autonomous: true
requirements:
  - EXP-01
  - REPR-01
user_setup: []
must_haves:
  truths:
    - "Researcher can run the required ablations and compare them under a stable experiment schema."
    - "Reported results can be reproduced from saved cohort, split, action, reward, model, and backend metadata."
  artifacts:
    - path: "src/mimic_sepsis_rl/evaluation/ablations.py"
      provides: "Ablation registry and execution helpers"
      min_lines: 50
    - path: "src/mimic_sepsis_rl/reporting/package.py"
      provides: "Reproducible experiment bundle assembly"
      min_lines: 50
    - path: "tests/reporting/test_repro_bundle.py"
      provides: "Coverage for required reproducibility metadata in outputs"
      min_lines: 25
    - path: "docs/reproducibility.md"
      provides: "Research-facing reproducibility checklist and bundle contents"
      min_lines: 30
  key_links:
    - from: "src/mimic_sepsis_rl/evaluation/ablations.py"
      to: "src/mimic_sepsis_rl/reporting/package.py"
      via: "Ablation outputs flow into the final reproducible report bundle"
    - from: "src/mimic_sepsis_rl/reporting/package.py"
      to: "docs/reproducibility.md"
      via: "Bundle contents and metadata expectations are documented"
---

<objective>
Finish the benchmark with ablations and a reproducible reporting bundle.

Purpose: Close Phase 9 by making the benchmark defensible for thesis or paper use, with explicit metadata and rerun instructions.
Output: Ablation registry, report-bundle assembler, tests, and reproducibility documentation.
</objective>

<execution_context>
@/home/furkan/.codex/get-shit-done/workflows/execute-plan.md
@/home/furkan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/phases/09-evaluation-safety-and-reproducible-package/09-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ablation registry and controlled comparison helpers</name>
  <files>src/mimic_sepsis_rl/evaluation/ablations.py, tests/evaluation/test_ablation_registry.py</files>
  <action>Implement a registry or config-driven surface for reward-shaping, action-granularity, timestep, missingness-flag, and feature-subset ablations. Tests should verify that ablation variants inherit the same experiment metadata structure and remain attributable to a clear benchmark version.</action>
  <verify>pytest -q tests/evaluation/test_ablation_registry.py</verify>
  <done>The required ablations can be launched and compared under a consistent schema.</done>
</task>

<task type="auto">
  <name>Task 2: Assemble reproducible reporting bundle and metadata checks</name>
  <files>src/mimic_sepsis_rl/reporting/package.py, tests/reporting/test_repro_bundle.py, docs/reproducibility.md</files>
  <action>Implement a report-bundle assembler that collects cohort specs, feature dictionaries, split manifests, action bins, reward versions, checkpoints, evaluation metrics, and recorded accelerator backend metadata. Add tests ensuring required metadata is present, and document how a researcher can rerun or audit the benchmark outputs.</action>
  <verify>pytest -q tests/reporting/test_repro_bundle.py</verify>
  <done>The benchmark can emit a reproducible package suitable for research review and reruns.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest -q tests/evaluation/test_ablation_registry.py tests/reporting/test_repro_bundle.py` passes
- [ ] Ablation registry covers reward, action, timestep, missingness, and feature-subset variants
- [ ] Reproducibility bundle includes accelerator backend metadata alongside experiment artifacts
</verification>

<success_criteria>

- All tasks completed
- EXP-01 and REPR-01 are satisfied with controlled ablations and a reproducible package
- The roadmap ends with research-grade outputs instead of isolated scripts and plots

</success_criteria>

<output>
After completion, create `.planning/phases/09-evaluation-safety-and-reproducible-package/09-02-SUMMARY.md`
</output>
