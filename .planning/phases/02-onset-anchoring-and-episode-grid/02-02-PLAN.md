---
phase: 02-onset-anchoring-and-episode-grid
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/mimic_sepsis_rl/data/episodes.py
  - src/mimic_sepsis_rl/data/episode_models.py
  - src/mimic_sepsis_rl/cli/build_episode_grid.py
  - tests/data/test_episode_grid.py
autonomous: true
requirements:
  - COH-03
user_setup: []
must_haves:
  truths:
    - "Researcher can materialize deterministic 4-hour windows from onset -24h to +48h."
    - "Truncated or unusable episodes are inspectable with explicit reasons."
  artifacts:
    - path: "src/mimic_sepsis_rl/data/episodes.py"
      provides: "Episode grid construction logic"
      min_lines: 50
    - path: "src/mimic_sepsis_rl/cli/build_episode_grid.py"
      provides: "CLI entrypoint for episode window materialization"
      min_lines: 30
    - path: "tests/data/test_episode_grid.py"
      provides: "Regression coverage for step boundaries and truncation"
      min_lines: 25
  key_links:
    - from: "src/mimic_sepsis_rl/data/episodes.py"
      to: "src/mimic_sepsis_rl/data/onset.py"
      via: "Episode windows are anchored to selected onset outputs"
---

<objective>
Build deterministic episode windows anchored to the chosen onset timestamps.

Purpose: Finish the time-axis contract for later state, action, and reward computation.
Output: Episode grid builder, typed episode-step models, CLI entrypoint, and tests for boundaries and truncation handling.
</objective>

<execution_context>
@/home/furkan/.codex/get-shit-done/workflows/execute-plan.md
@/home/furkan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/phases/02-onset-anchoring-and-episode-grid/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement 4-hour episode grid generation and models</name>
  <files>src/mimic_sepsis_rl/data/episodes.py, src/mimic_sepsis_rl/data/episode_models.py</files>
  <action>Implement deterministic generation of 4-hour episode steps from `onset -24h` through `onset +48h`, including stable step indexing, start and end timestamps, and truncation metadata for early ICU discharge or death. Keep unusable episodes explicit rather than filtering them away without trace.</action>
  <verify>python -m mimic_sepsis_rl.cli.build_episode_grid --dry-run</verify>
  <done>Episode-step rows can be generated consistently with explicit step boundaries and truncation reasons.</done>
</task>

<task type="auto">
  <name>Task 2: Add CLI and tests for deterministic boundary handling</name>
  <files>src/mimic_sepsis_rl/cli/build_episode_grid.py, tests/data/test_episode_grid.py</files>
  <action>Create a CLI entrypoint that consumes onset outputs and writes or previews the episode grid. Add tests verifying the expected step count, start/end alignment, and truncation behavior for edge cases such as short stays and unusable episodes.</action>
  <verify>pytest -q tests/data/test_episode_grid.py && python -m mimic_sepsis_rl.cli.build_episode_grid --dry-run</verify>
  <done>The time grid is invokable, test-covered, and stable enough for split creation and feature work.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest -q tests/data/test_episode_grid.py` passes
- [ ] `python -m mimic_sepsis_rl.cli.build_episode_grid --dry-run` completes with stable step indices
- [ ] The generated grid respects the `-24h/+48h` window and 4-hour timestep contract
</verification>

<success_criteria>

- All tasks completed
- COH-03 is satisfied through deterministic episode window generation
- Phase 2 outputs are reusable by Phase 3 and Phase 4 without manual cleanup

</success_criteria>

<output>
After completion, create `.planning/phases/02-onset-anchoring-and-episode-grid/02-02-SUMMARY.md`
</output>
