---
phase: 02-onset-anchoring-and-episode-grid
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mimic_sepsis_rl/data/onset.py
  - src/mimic_sepsis_rl/data/onset_models.py
  - configs/onset/default.yaml
  - tests/data/test_onset_assignment.py
autonomous: true
requirements:
  - COH-02
user_setup: []
must_haves:
  truths:
    - "Each usable ICU episode gets one onset timestamp or an explicit unusable flag."
    - "Ambiguous onset candidates are surfaced rather than silently accepted."
  artifacts:
    - path: "src/mimic_sepsis_rl/data/onset.py"
      provides: "Sepsis onset assignment pipeline"
      min_lines: 50
    - path: "configs/onset/default.yaml"
      provides: "Operationalized onset parameters"
      contains: "lookback_hours"
    - path: "tests/data/test_onset_assignment.py"
      provides: "Regression coverage for onset uniqueness and unusable cases"
      min_lines: 25
  key_links:
    - from: "src/mimic_sepsis_rl/data/onset.py"
      to: "src/mimic_sepsis_rl/data/onset_models.py"
      via: "Assignment pipeline emits typed onset results"
---

<objective>
Operationalize Sepsis-3 onset assignment for each cohort episode.

Purpose: Establish the single time anchor that all downstream windowing, features, and rewards depend on.
Output: Onset assignment module, typed onset outputs, config, and tests for uniqueness and unusable handling.
</objective>

<execution_context>
@/home/furkan/.codex/get-shit-done/workflows/execute-plan.md
@/home/furkan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/phases/02-onset-anchoring-and-episode-grid/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement onset candidate extraction and uniqueness rules</name>
  <files>src/mimic_sepsis_rl/data/onset.py, src/mimic_sepsis_rl/data/onset_models.py, configs/onset/default.yaml</files>
  <action>Implement onset candidate extraction based on the project's Sepsis-3 operationalization, including candidate scoring or prioritization rules, unusable-state handling, and explicit typed outputs for selected onset, rejected candidates, and unusable episodes. Keep parameters externalized in config so phase reviews can tune logic without hidden code edits.</action>
  <verify>python -m mimic_sepsis_rl.data.onset --config configs/onset/default.yaml --dry-run</verify>
  <done>Onset assignment can produce one selected onset or an unusable marker for every cohort episode.</done>
</task>

<task type="auto">
  <name>Task 2: Add tests for ambiguous and unusable onset scenarios</name>
  <files>tests/data/test_onset_assignment.py</files>
  <action>Write tests covering straightforward onset selection, conflicting candidates, and no-valid-onset scenarios. Ensure the pipeline never returns multiple accepted onset timestamps for the same ICU episode.</action>
  <verify>pytest -q tests/data/test_onset_assignment.py</verify>
  <done>Ambiguous and unusable onset cases are explicitly covered by tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest -q tests/data/test_onset_assignment.py` passes
- [ ] `python -m mimic_sepsis_rl.data.onset --config configs/onset/default.yaml --dry-run` completes with typed outputs
- [ ] No tested scenario yields more than one selected onset per ICU episode
</verification>

<success_criteria>

- All tasks completed
- COH-02 is satisfied through reproducible one-onset-or-unusable assignment logic
- Phase 2 now has a stable onset anchor for episode window generation

</success_criteria>

<output>
After completion, create `.planning/phases/02-onset-anchoring-and-episode-grid/02-01-SUMMARY.md`
</output>
