---
phase: 01-cohort-definition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mimic_sepsis_rl/data/cohort/spec.py
  - src/mimic_sepsis_rl/cli/build_cohort.py
  - configs/cohort/default.yaml
  - docs/cohort_selection.md
  - tests/data/test_cohort_spec.py
autonomous: true
requirements:
  - COH-01
user_setup: []
must_haves:
  truths:
    - "Researcher can generate an adult ICU Sepsis-3 cohort from documented rules."
    - "Cohort rules are inspectable without reading ad hoc notebook code."
  artifacts:
    - path: "src/mimic_sepsis_rl/data/cohort/spec.py"
      provides: "Versioned cohort inclusion and exclusion rule definitions"
      min_lines: 40
    - path: "configs/cohort/default.yaml"
      provides: "Executable cohort configuration"
      contains: "adult_only"
    - path: "docs/cohort_selection.md"
      provides: "Readable cohort specification for research reporting"
      min_lines: 30
  key_links:
    - from: "src/mimic_sepsis_rl/cli/build_cohort.py"
      to: "configs/cohort/default.yaml"
      via: "CLI loads cohort config"
    - from: "docs/cohort_selection.md"
      to: "src/mimic_sepsis_rl/data/cohort/spec.py"
      via: "Documentation mirrors executable rules"
---

<objective>
Create the executable cohort definition surface for the project.

Purpose: Lock the adult ICU Sepsis-3 eligibility rules in code and config before any onset or feature work begins.
Output: Cohort rule module, cohort build CLI scaffold, default config, and written cohort specification.
</objective>

<execution_context>
@/home/furkan/.codex/get-shit-done/workflows/execute-plan.md
@/home/furkan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/phases/01-cohort-definition/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Encode cohort selection rules as executable spec</name>
  <files>src/mimic_sepsis_rl/data/cohort/spec.py, configs/cohort/default.yaml</files>
  <action>Define the adult ICU Sepsis-3 cohort contract as explicit typed rules and configuration values. Capture inclusion criteria, exclusion criteria, canonical identifier columns, and rule ordering so the same definition can drive both SQL extraction and audit reporting. Avoid notebook-only constants or logic hidden in shell scripts.</action>
  <verify>python -m mimic_sepsis_rl.cli.build_cohort --config configs/cohort/default.yaml --dry-run</verify>
  <done>Cohort rules are centralized, importable, and configurable from a default YAML file.</done>
</task>

<task type="auto">
  <name>Task 2: Add cohort builder entrypoint and research-facing documentation</name>
  <files>src/mimic_sepsis_rl/cli/build_cohort.py, docs/cohort_selection.md</files>
  <action>Create a CLI entrypoint that loads the cohort spec/config and exposes a dry-run path for later execution. Write `docs/cohort_selection.md` in language suitable for thesis or paper methods, including rule intent, required source tables, and the outputs this phase promises. Keep implementation details aligned with the documented rules.</action>
  <verify>ruff check src/mimic_sepsis_rl/data/cohort/spec.py src/mimic_sepsis_rl/cli/build_cohort.py && python -m mimic_sepsis_rl.cli.build_cohort --help</verify>
  <done>The project has a documented and invokable cohort selection entrypoint with no ambiguity about scope.</done>
</task>

<task type="auto">
  <name>Task 3: Add guardrail tests for rule completeness</name>
  <files>tests/data/test_cohort_spec.py</files>
  <action>Write focused tests that validate the cohort spec exposes adult-only gating, ICU stay scope, and explicit inclusion/exclusion categories. Tests should fail if future edits silently remove required rule sections or config bindings.</action>
  <verify>pytest -q tests/data/test_cohort_spec.py</verify>
  <done>Tests catch missing or incomplete cohort rule definitions before extraction work expands.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest -q tests/data/test_cohort_spec.py` passes
- [ ] `ruff check src/mimic_sepsis_rl/data/cohort/spec.py src/mimic_sepsis_rl/cli/build_cohort.py` passes
- [ ] `python -m mimic_sepsis_rl.cli.build_cohort --config configs/cohort/default.yaml --dry-run` completes without missing-config errors
</verification>

<success_criteria>

- All tasks completed
- Cohort rule logic is centralized and documented
- Future extraction work has a stable CLI/config entrypoint
- COH-01 is partially delivered through executable and human-readable cohort definitions

</success_criteria>

<output>
After completion, create `.planning/phases/01-cohort-definition/01-01-SUMMARY.md`
</output>
