---
phase: 01-cohort-definition
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/mimic_sepsis_rl/data/cohort/extract.py
  - src/mimic_sepsis_rl/data/cohort/audit.py
  - src/mimic_sepsis_rl/data/cohort/models.py
  - tests/data/test_cohort_audit.py
autonomous: true
requirements:
  - COH-01
user_setup: []
must_haves:
  truths:
    - "Researcher can inspect why each stay was included or excluded."
    - "Cohort extraction produces an auditable artifact instead of opaque side effects."
  artifacts:
    - path: "src/mimic_sepsis_rl/data/cohort/extract.py"
      provides: "Cohort extraction orchestration"
      min_lines: 50
    - path: "src/mimic_sepsis_rl/data/cohort/audit.py"
      provides: "Inclusion and exclusion audit reporting"
      min_lines: 40
    - path: "tests/data/test_cohort_audit.py"
      provides: "Regression coverage for audit outputs"
      min_lines: 25
  key_links:
    - from: "src/mimic_sepsis_rl/data/cohort/extract.py"
      to: "src/mimic_sepsis_rl/data/cohort/audit.py"
      via: "Extraction emits audit rows for every decision"
---

<objective>
Build the auditable cohort extraction workflow on top of the cohort spec.

Purpose: Finish Phase 1 by ensuring the study population can be generated with transparent inclusion and exclusion reporting.
Output: Cohort extraction orchestrator, audit models/reporting, and regression tests for audit completeness.
</objective>

<execution_context>
@/home/furkan/.codex/get-shit-done/workflows/execute-plan.md
@/home/furkan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/phases/01-cohort-definition/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement cohort extraction orchestration and output models</name>
  <files>src/mimic_sepsis_rl/data/cohort/extract.py, src/mimic_sepsis_rl/data/cohort/models.py</files>
  <action>Implement the cohort extraction service and typed output models that convert source query results into a stable cohort artifact. Define explicit schemas for included stays, excluded stays, and per-rule audit records so later phases can depend on stable identifiers and metadata.</action>
  <verify>python -m mimic_sepsis_rl.cli.build_cohort --config configs/cohort/default.yaml --emit-audit --dry-run</verify>
  <done>Cohort extraction returns structured included/excluded outputs with stable keys.</done>
</task>

<task type="auto">
  <name>Task 2: Add audit reporting and exclusion-reason coverage</name>
  <files>src/mimic_sepsis_rl/data/cohort/audit.py, tests/data/test_cohort_audit.py</files>
  <action>Create audit helpers that summarize inclusion and exclusion counts, preserve row-level decisions, and surface missing-rule situations as errors. Add regression tests that verify excluded rows receive reasons and included rows remain traceable back to the final cohort artifact.</action>
  <verify>pytest -q tests/data/test_cohort_audit.py</verify>
  <done>The extraction path produces auditable, test-covered inclusion and exclusion outputs.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest -q tests/data/test_cohort_audit.py` passes
- [ ] `python -m mimic_sepsis_rl.cli.build_cohort --config configs/cohort/default.yaml --emit-audit --dry-run` emits included and excluded summaries
- [ ] Included and excluded artifacts share stable identifier columns defined in the cohort models
</verification>

<success_criteria>

- All tasks completed
- Phase 1 can produce the cohort with inspectable inclusion and exclusion reasoning
- Audit outputs are regression-tested and reusable by Phase 2

</success_criteria>

<output>
After completion, create `.planning/phases/01-cohort-definition/01-02-SUMMARY.md`
</output>
