---
phase: 03-split-manifests-and-leakage-boundaries
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mimic_sepsis_rl/data/splits.py
  - src/mimic_sepsis_rl/data/split_models.py
  - configs/splits/default.yaml
  - tests/data/test_split_manifests.py
  - docs/leakage_boundaries.md
autonomous: true
requirements:
  - DATA-02
user_setup: []
must_haves:
  truths:
    - "Researcher can generate patient-level train, validation, and test manifests with fixed seeds."
    - "No patient appears in more than one split."
    - "Downstream stages have a canonical train-only fit boundary."
  artifacts:
    - path: "src/mimic_sepsis_rl/data/splits.py"
      provides: "Patient-level split generation and validation"
      min_lines: 60
    - path: "tests/data/test_split_manifests.py"
      provides: "Leakage checks for duplicate patients across splits"
      min_lines: 30
    - path: "docs/leakage_boundaries.md"
      provides: "Train-only fit contract for downstream phases"
      min_lines: 25
  key_links:
    - from: "src/mimic_sepsis_rl/data/splits.py"
      to: "docs/leakage_boundaries.md"
      via: "Implementation and written leakage policy stay aligned"
---

<objective>
Freeze patient-level data partitions and leakage boundaries before feature preprocessing begins.

Purpose: Protect every learned downstream transform from accidental information leakage.
Output: Split generation module, split config, regression tests, and a written train-only fit contract.
</objective>

<execution_context>
@/home/furkan/.codex/get-shit-done/workflows/execute-plan.md
@/home/furkan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/phases/03-split-manifests-and-leakage-boundaries/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement patient-level split manifest generation</name>
  <files>src/mimic_sepsis_rl/data/splits.py, src/mimic_sepsis_rl/data/split_models.py, configs/splits/default.yaml</files>
  <action>Implement split generation that operates at the patient identifier level, persists fixed seeds, and emits manifest artifacts linking patients and eligible episode keys. Include helpers for split summary statistics so future reviews can inspect mortality balance and sample counts without re-running notebooks.</action>
  <verify>python -m mimic_sepsis_rl.data.splits --config configs/splits/default.yaml --dry-run</verify>
  <done>Train, validation, and test manifests can be generated reproducibly from the episode set.</done>
</task>

<task type="auto">
  <name>Task 2: Add duplicate-patient leakage tests and downstream contract docs</name>
  <files>tests/data/test_split_manifests.py, docs/leakage_boundaries.md</files>
  <action>Write tests that fail when a patient appears in multiple splits or when manifest metadata omits the seed and source episode set. Document the downstream rule that scalers, bin thresholds, imputers, and behavior estimators must fit on train only and consume these manifests as the boundary.</action>
  <verify>pytest -q tests/data/test_split_manifests.py</verify>
  <done>Leakage-sensitive assumptions are enforced by tests and documented for all future phases.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest -q tests/data/test_split_manifests.py` passes
- [ ] `python -m mimic_sepsis_rl.data.splits --config configs/splits/default.yaml --dry-run` emits deterministic manifest metadata
- [ ] No test fixture allows the same patient identifier in more than one split
</verification>

<success_criteria>

- All tasks completed
- DATA-02 is satisfied with saved, test-covered patient-level split manifests
- Phase 4 and later phases have an explicit train-only fit boundary

</success_criteria>

<output>
After completion, create `.planning/phases/03-split-manifests-and-leakage-boundaries/03-01-SUMMARY.md`
</output>
