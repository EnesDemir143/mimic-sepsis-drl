---
phase: 07-cql-reference-training
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mimic_sepsis_rl/training/device.py
  - src/mimic_sepsis_rl/training/config.py
  - configs/training/runtime.mps.yaml
  - configs/training/runtime.cuda.yaml
  - tests/training/test_device_runtime.py
autonomous: true
requirements:
  - PLAT-01
user_setup: []
must_haves:
  truths:
    - "The same training and evaluation entrypoint can select `MPS` or `CUDA` without forking core logic."
    - "Backend metadata is recorded so runs remain reproducible across devices."
  artifacts:
    - path: "src/mimic_sepsis_rl/training/device.py"
      provides: "Device selection and fallback abstraction"
      min_lines: 50
    - path: "configs/training/runtime.mps.yaml"
      provides: "Apple Silicon runtime configuration"
      contains: "device: mps"
    - path: "configs/training/runtime.cuda.yaml"
      provides: "NVIDIA CUDA runtime configuration"
      contains: "device: cuda"
    - path: "tests/training/test_device_runtime.py"
      provides: "Coverage for runtime selection and fallback behavior"
      min_lines: 25
  key_links:
    - from: "src/mimic_sepsis_rl/training/config.py"
      to: "src/mimic_sepsis_rl/training/device.py"
      via: "Training config resolves runtime device through one shared abstraction"
---

<objective>
Build the cross-platform runtime abstraction required before CQL training begins.

Purpose: Satisfy the portability requirement so Phase 7's trainer runs on Apple Silicon `MPS` and NVIDIA `CUDA` without diverging code paths.
Output: Device abstraction module, per-backend runtime configs, and tests for selection and fallback behavior.
</objective>

<execution_context>
@/home/furkan/.codex/get-shit-done/workflows/execute-plan.md
@/home/furkan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/phases/07-cql-reference-training/07-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement one shared runtime abstraction for CPU, MPS, and CUDA</name>
  <files>src/mimic_sepsis_rl/training/device.py, src/mimic_sepsis_rl/training/config.py</files>
  <action>Create a device selection layer that detects or accepts configured accelerator choices, validates availability, and falls back cleanly when unsupported. Keep algorithm code unaware of backend-specific branching beyond this abstraction, and record backend details in run metadata for later reproducibility.</action>
  <verify>python -m mimic_sepsis_rl.training.device --self-check</verify>
  <done>A single runtime abstraction can resolve the effective device and expose reproducible metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Add backend-specific runtime configs and regression tests</name>
  <files>configs/training/runtime.mps.yaml, configs/training/runtime.cuda.yaml, tests/training/test_device_runtime.py</files>
  <action>Create runtime configuration files for Apple Silicon `MPS` and NVIDIA `CUDA`, and write tests for device selection, unavailable-backend fallback, and metadata recording. Include explicit checks for unsupported-op fallback behavior rather than hiding failures until later experiments.</action>
  <verify>pytest -q tests/training/test_device_runtime.py</verify>
  <done>The project can select and validate supported accelerator backends before any algorithm-specific trainer runs.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest -q tests/training/test_device_runtime.py` passes
- [ ] `python -m mimic_sepsis_rl.training.device --self-check` reports consistent backend metadata
- [ ] Runtime configs exist for both `mps` and `cuda`
</verification>

<success_criteria>

- All tasks completed
- PLAT-01 is satisfied through one shared runtime path for MPS and CUDA
- CQL implementation can build on a tested, device-agnostic training foundation

</success_criteria>

<output>
After completion, create `.planning/phases/07-cql-reference-training/07-01-SUMMARY.md`
</output>
