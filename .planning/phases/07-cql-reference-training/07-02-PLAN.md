---
phase: 07-cql-reference-training
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/mimic_sepsis_rl/training/cql.py
  - src/mimic_sepsis_rl/training/common.py
  - configs/training/cql.yaml
  - tests/training/test_cql_pipeline.py
  - docs/cql_training.md
autonomous: true
requirements:
  - RL-01
user_setup: []
must_haves:
  truths:
    - "Researcher can train a discrete-action CQL policy on the prepared offline dataset."
    - "The CQL run uses the frozen dataset contract and runtime abstraction rather than hidden custom preprocessing."
  artifacts:
    - path: "src/mimic_sepsis_rl/training/cql.py"
      provides: "CQL trainer and inference loading surface"
      min_lines: 70
    - path: "configs/training/cql.yaml"
      provides: "Reusable CQL training configuration"
      contains: "algorithm: cql"
    - path: "tests/training/test_cql_pipeline.py"
      provides: "Coverage for dataset wiring and saved model loading"
      min_lines: 30
  key_links:
    - from: "src/mimic_sepsis_rl/training/cql.py"
      to: "src/mimic_sepsis_rl/datasets/replay_buffer.py"
      via: "CQL consumes the shared replay dataset contract"
    - from: "src/mimic_sepsis_rl/training/cql.py"
      to: "src/mimic_sepsis_rl/training/device.py"
      via: "CQL trainer resolves device through the shared runtime abstraction"
---

<objective>
Implement the reference CQL training pipeline on top of the cross-platform runtime layer.

Purpose: Deliver the first end-to-end offline RL algorithm using the finalized dataset contract and runtime abstraction.
Output: CQL trainer, shared training utilities, config, regression tests, and training documentation.
</objective>

<execution_context>
@/home/furkan/.codex/get-shit-done/workflows/execute-plan.md
@/home/furkan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/phases/07-cql-reference-training/07-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement shared training helpers and discrete CQL trainer</name>
  <files>src/mimic_sepsis_rl/training/common.py, src/mimic_sepsis_rl/training/cql.py, configs/training/cql.yaml</files>
  <action>Implement common training utilities for dataset loading, checkpointing, metric logging, and config resolution, then build the discrete-action CQL trainer on top of them. Ensure the trainer consumes the frozen replay dataset contract and routes device resolution through the shared runtime abstraction from Plan 07-01.</action>
  <verify>python -m mimic_sepsis_rl.training.cql --config configs/training/cql.yaml --dry-run</verify>
  <done>The project can launch a reproducible CQL training run against the prepared offline dataset.</done>
</task>

<task type="auto">
  <name>Task 2: Add pipeline tests and operator-facing training docs</name>
  <files>tests/training/test_cql_pipeline.py, docs/cql_training.md</files>
  <action>Write tests that verify the CQL trainer accepts replay-buffer inputs, writes checkpoints, and can reload a trained policy for held-out inference. Document expected config fields, outputs, and portability constraints so later BCQ/IQL work reuses this foundation instead of bypassing it.</action>
  <verify>pytest -q tests/training/test_cql_pipeline.py</verify>
  <done>CQL training is test-covered, documented, and ready to act as the reference RL path.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest -q tests/training/test_cql_pipeline.py` passes
- [ ] `python -m mimic_sepsis_rl.training.cql --config configs/training/cql.yaml --dry-run` completes with the shared runtime abstraction
- [ ] A saved CQL checkpoint can be reloaded for held-out inference in tests
</verification>

<success_criteria>

- All tasks completed
- RL-01 is satisfied through an end-to-end CQL training surface
- Phase 8 can reuse the shared training helpers and runtime abstraction

</success_criteria>

<output>
After completion, create `.planning/phases/07-cql-reference-training/07-02-SUMMARY.md`
</output>
